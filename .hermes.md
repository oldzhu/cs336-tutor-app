# CS336 Tutor App — Hermes-Specific Rules

> Auto-injected by Hermes Agent.
> **Cross-tool rules → see `AGENTS.md`** (works with OpenCode, Claude Code, Codex too).

## RULE 1: Use AGENTS.md for shared rules
All project-wide rules live in `AGENTS.md` which is read by all agentic tools.
`.hermes.md` is for Hermes-only configuration.

## RULE 2: OpenCode for development
Use `opencode run` from WSL (`/home/oldzhu/cs336-tutor-app`) for all code generation.
Resume last session: `opencode -c`

## WSL Environment
```bash
# Access WSL from Hermes terminal
wsl -d myUbuntu -- bash -c '...'
# Key env vars for WSL commands:
export http_proxy="http://172.20.80.1:10808"
export https_proxy="http://172.20.80.1:10808"
export ANDROID_HOME=~/Android/Sdk
```

## Bilingual Docs
Docs live in docs/spec/, docs/plan/, docs/log/ in both en/ and zh/.
Update both languages for every change. See AGENTS.md Rule 4 for details.
